name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - run: |
          cd ~/projects/hikvsion_get_events
          git pull
          docker compose up -d --build
